import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  boxContainer: {
    position: 'relative',
    overflow: 'hidden',
  },
  ripple: {
    borderRadius: 999999,
    position: 'absolute',
  },
});
