import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    position: 'relative',
    overflow: 'hidden',
  },
  ripple: {
    borderRadius: 999,
    position: 'absolute',
  },
});
